{
  "name": "workflow-sample",
  "version": "1.0.1",
  "type": "module",
  "description": "This repository provides an application sample for graph handling of Flowbuild artifact 'blueprint' ",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "npm i -g npm-check-updates && ncu -u && npm update"
  },
  "author": "@brunolnetto",
  "license": "MIT",
  "dependencies": {
    "dot-quiver": "^3.0.1",
    "es-iter": "^1.1.2",
    "express": "^4.17.2",
    "fs": "^0.0.1-security",
    "nodemon": "^2.0.15"
  }
}
