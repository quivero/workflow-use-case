{
  "name": "test-dot-quiver",
  "version": "1.0.1",
  "type": "module",
  "description": "dot-quiver use case",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "npm i -g npm-check-updates && ncu -u && npm update"
  },
  "author": "@brunolnetto",
  "license": "MIT",
  "dependencies": {
    "dot-quiver": "^3.0.1",
    "express": "^4.17.2",
    "fs": "^0.0.1-security",
    "nodemon": "^2.0.15"
  }
}
